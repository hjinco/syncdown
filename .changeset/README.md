## Changesets

Add a changeset for user-facing CLI changes before merging a PR:

```sh
bun run changeset
```

Select `@syncdown/cli`, choose the appropriate bump type, and summarize the user-visible change.
